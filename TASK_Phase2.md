---

## 3. 女巨人追逐与“踩小人”细节（玩家是小人，被追着踩）

### 3.1 女巨人 AI 状态机（`GiantessBrain`）
- 状态：`Idle/Patrol → Detect(发现) → Chase(追) → ApproachAndWindup(逼近+抬脚预备) → Stomp(踩下) → 复位`。
- **移动匹配步态**：步速/方向与步态节奏一致，避免“滑冰”。可用一段循环待机/走路 VMD 驱动下半身（若用户给），否则用程序在骨层级上做轻微步态。禁止生硬瞬移。
- **俯视盯人**：追你时头骨下压、视线跟向玩家（“追着踩”的观感关键）。
- 玩家太近 → 跟前抬脚踩。

### 3.2 踩踏判定（`StompDetector`）——必须可靠、自然、不过度
- **脚掌碰撞体挂脚（跟随足首/脚掌骨）**，随骨更新世界位。
- **两段判定都成立才命中**：① 该脚处于“踩下”动作且 y 持续下降；② 玩家中心投影在该脚掌平面范围内。
- 命中给玩家**短暂保护（如 2s 免伤）**，避免一帧连续判定秒杀。
- **无敌模式**：开启时踩到仅视觉反馈（扬灰/震动），玩家不死无惩罚；关闭时踩到=命中 → 玩家被击退或重生安全点（沙盒推荐重生，不弹强制 Game Over）。
- 命中要有一点预备动画与延迟，让人“看得清、躲得开”，别随机秒。
- 女巨人脚碰地对楼顶/地面不穿模（城市楼体给 Collider；可让矮楼顶被踩塌可选）。

### 3.3 小人（TinyPlayer）
- 程序生成外观：胶囊/简单人形/Primitive 组合，无需 pmx。
- 移动：虚拟摇杆（左）+ 视角拖动（右）或简化。能跑/躲。
- 被踩判定见 §3.2；命中反馈=短暂击退+闪红+提示。

---

## 4. 物理质量（本任务最强调：自然、不飞、合理）
用户原话：“注意物理，不要衣服乱飞，要自然，物理必须必须做好、合理。”

### 4.1 女巨人裙摆/头发/衣服物理自然的关键守则（写进代码与文档）
1. **不放大模型**，改缩世界（§0.1）。放大是衣服乱飞头号元凶。
2. `MMDTransformManager.livePhysics = true`；确认 `MMDPhysicsManager` 与地面碰撞存在。物理仅在支持平台跑，安卓 arm64 OK。
3. **地面要有碰撞**，裙摆刚体不与地面穿模；城市楼体给 Collider。
4. **物理步长/稳定**：UMT 拉 `targetFrameRate` 到 60；别把物理时间步调过大致抖，别多帧跨步。
5. 若用户模型裙子因刚体参数差而抖/飞：提供「布料物理强度/关闭」调节或安全参数；但**默认先靠“世界缩放到位+地面碰撞+正确 livePhysics”做对**，别用“关物理”掩盖。
6. 真机抖动：提醒可能来自该 pmx 刚体参数或渲染/相机，`MMDSDEFSkinning` 保持开启。
7. **验收观感**：女巨人走动/追你时裙摆头发自然跟随摆动，不飘出体外、不撕裂、不原地乱颤。

### 4.2 世界缩放稳定
- `WorldScaler`：整城+小人放一个根节点统一缩放，数值进 CONFIG 可调，默认到“玩家≈女巨人脚趾”。女巨人本身不缩放。
- 缩放后碰撞体厚度/脚掌判定在小尺度仍稳；必要时用 double 或居中偏移避免浮点问题。

---

## 5. 城市生成（`CityGenerator`）
- 程序生成一片**低模街区**：若干楼房（Box+简单材质/贴色）、马路、路灯/小道具；**全部带 Collider**（玩家可跑不穿墙；女巨人矮楼顶可落脚/压塌可选）。
- 给玩家逃窜空间：小巷、楼房间隙、转角。先做地面跑位+可躲角落。
- 参数进 CONFIG（街区尺寸、楼高/密、路宽），可一键重生成。
- 城市放小世界根下随 `WorldScaler` 缩放。

---

## 6. 无敌模式与沙盒（用户明确要求）
- 提供 **无敌开关（Invincible toggle）**，UI 一键切换。
- **无敌 ON**：被踩到不死无惩罚，仅视觉/震动反馈。
- **无敌 OFF（沙盒默认）**：被踩到=命中 → 重生附近安全点（不清城市、女巨人仍在）。不建议强制 Game Over（你要沙盒）。
- 加「重置/换位置」按钮方便反复玩。
- 开关数值进 CONFIG。

---

## 7. 安卓性能（别忽略）
- 手机跑：控制同屏面数/合批；城市低模；粒子/后效克制。女巨人 pmx 可能很大（大贴图），注意显存/内存，给必要时 mipmap/压缩贴图开关。
- 物理+渲染在真机尽量稳 60（UMT 锁 60）。帧率不足先降城市密度/粒子，再降贴图，别动物理步长。

---

## 8. 你不能运行 Unity/安卓真机 —— 诚实与交付
- 沿用 Phase1 纪律：**禁止声称已测试**；只做静态自查；按报告格式交付。
- 把「哪些必须用户在安卓真机+自己模型上验证」列清楚：模型选择/贴图读取/物理是否自然/无敌是否生效/性能。
- 报告给出：安卓构建步骤 + 女巨人 pmx 放手机哪条路径 + 一步步验证清单。
- 贴图运行时读取、SAF 选文件、缩放数值等只能真机定的，标「待真机验证/待定默认值」，别编数值糊弄。

---

## 9. 本次范围边界
**做**：Phase2 全玩法（沙盒追逐+无敌+城市+程序小人+女巨人手机导入与更换+自然物理）。
**不做/后续**：60 秒计分/难度曲线、正式 UI/菜单美化、lilToon 渲染调优、多玩家/联机、玩家也用自选 pmx（本版玩家=程序小人，女巨人才是可换 pmx）、正式存档/设置页。

## 10. 收尾
复用并保留 Phase1 一切代码；新增 Phase2 全部文件；自查后 commit（信息列出新增脚本/场景），push 回 `main`。禁止半成品交付；完不成的如实说明，别假装完成。
